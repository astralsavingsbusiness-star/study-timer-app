// Temporary placeholder for the MCP server.
// We will replace this with real code in the next steps.
export default async function handler(req, res) {
  res.status(200).json({ ok: true, message: "Study timer MCP placeholder" });
}
